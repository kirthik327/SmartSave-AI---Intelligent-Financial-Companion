{
  "name": "smartsave-ai-monorepo",
  "version": "1.0.0",
  "description": "SmartSave AI - Premium AI Financial Companion Monorepo",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently --kill-others \"npm run backend\" \"npm run frontend\"",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && mv frontend/dist dist",
    "start": "npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
